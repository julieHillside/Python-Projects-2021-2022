# on a mac open a terminal

python3 -m pip install --user pygame

python3 -m --version

# on windows go into the Thonny IDE,
# push ctrl+t

py --version
py -m pip --version
py -m pip install --user pygame

# find the python.exe path and put it into
# Thonny
py -m --version